# דוח ניקוי ג׳יבריש רוחבי — 2026-05-10

## מטרת הבדיקה

בוצעה בדיקה רוחבית לאיתור טקסט משובש, שאריות יבוא, רצפי סימני שאלה, תווי replacement, ערכי debug ותוכן לא קריא בדפי האתר ובקובצי הנתונים המרכזיים.

העבודה בוצעה על branch:

`codex/fix-gibberish-sitewide-20260510`

לא בוצע שינוי production.

## קבצים ואזורים שנבדקו

- דפי שיעור מרכזיים: שיעור 1, שיעור 2, חוק ארגון הפיקוח, פקודת הבטיחות, ISO 45001.
- דפי אינדקס: חוקים, תקנים, כלי שטח, סילבוס, תרגול, שאלות למבחן ואתגר בטיחות.
- קובצי נתונים: `data/search-index.js`, `data/exam-questions.js`, `data/game-data.js`.
- בדיקת דפדפן מקומית לדפים מרכזיים לאחר טעינת JavaScript.

## ג׳יבריש שנמצא ותוקן

| מיקום | הבעיה | תיקון |
| --- | --- | --- |
| `pages/field-tools.html` | פסקאות עם רצפי `???` במקום עברית | הוחלפו לניסוח עברי תקין המתאר את אזור כלי השטח והתקנים |
| `pages/laws.html` | טקסט הסבר משובש באזור היררכיית מקורות הדין ובכרטיס בדיקת תקן | הוחלף להסבר עברי תקין וזהיר |
| `pages/lesson-01.html` | כותרות accordion שנוצרו כרצפי `???` | שוחזרו מכותרות/הערות הפרק הקיימות ללא שינוי תוכן לימודי |
| `pages/lesson-02.html` | כותרות ופסקאות הסבר משובשות | שוחזרו לניסוח עברי תקין, עם הפניות לשיעור 1 כאשר נדרש פירוט נוסף |
| `pages/iso-45001-2018.html` | כותרות מסגרת שנוצרו כרצפי `???` | שוחזרו מתוך הערות הפרק הקיימות |
| `data/search-index.js` | אינדקס חיפוש עם mojibake ורצפי `???` | נבנה מחדש כאינדקס חיפוש נקי ותמציתי בעברית |

## מה הוסר

- רצפי `???` גלויים בתוכן האתר.
- טקסטי placeholder שנוצרו כתוצאה מייבוא לא תקין.
- שאריות mojibake באינדקס החיפוש המקומי.

לא נמחק תוכן לימודי מהותי.

## מה הוחלף במשפט ניטרלי

לא נדרש שימוש במשפט ניטרלי מסוג "נדרש להשלים מידע מקצועי לאחר בדיקה מול מקור מוסמך", משום שבכל המקרים שנמצאו היה ברור מהו ההקשר הלימודי או שניתן היה לשחזר את הכותרת מתוך התוכן הקיים.

## מה דורש בדיקה ידנית

- מומלץ לבצע מעבר ויזואלי ידני נוסף על הדפים הארוכים לאחר deploy ל־staging, בעיקר כדי לוודא שהכותרות ששוחזרו מתוך הערות הפרקים נוחות לקריאה למשתמש.
- לא זוהה תוכן משפטי שסותר חוק/פקודה/תקן במסגרת תיקון זה.

## בדיקות שבוצעו

- `node --check scripts/check-gibberish-content.js`
- `node --check scripts/rebuild-clean-search-index.js`
- `node --check data/search-index.js`
- `node scripts/check-gibberish-content.js`
- `node scripts/check-interactive-controls.js`
- חיפוש גלובלי אחר: `???`, `�`, `ג€`, `â€`, `[object Object]`, `NaN`

תוצאת בדיקת הג׳יבריש:

`Gibberish scan passed`

דוח מכונה נשמר ב:

`docs/gibberish-content-scan.json`

## אישור סביבתי

- לא בוצע deploy ל־production.
- לא שונו `firestore.rules`, `.firebaserc`, `js/auth.js`, `js/firebase-config.js`.
- העבודה מיועדת ל־staging בלבד.
